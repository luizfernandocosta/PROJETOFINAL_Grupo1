inmet_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'edp') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'edp123') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'weather_dw') }}"
      schema: gold
      threads: 4
      keepalives_idle: 0
      connect_timeout: 30
